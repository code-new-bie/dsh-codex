diff --git a/codex-rs/tui/src/main.rs b/codex-rs/tui/src/main.rs
--- a/codex-rs/tui/src/main.rs
+++ b/codex-rs/tui/src/main.rs
@@ -4,6 +4,7 @@ use codex_arg0::arg0_dispatch_or_else;
 use codex_config::LoaderOverrides;
 use codex_tui::AppExitInfo;
 use codex_tui::Cli;
 use codex_tui::ExitReason;
+use codex_tui::resolve_remote_addr;
 use codex_tui::run_main;
 use codex_utils_cli::CliConfigOverrides;
 use std::io::Write;
@@ -45,6 +46,11 @@ fn main() -> anyhow::Result<()> {
         inner
             .config_overrides
             .raw_overrides
             .splice(0..0, top_cli.config_overrides.raw_overrides);
+        let dshx_endpoint = std::env::var("DSHX_APP_SERVER_ENDPOINT")
+            .map_err(|_| anyhow::anyhow!(
+                "DSHX_APP_SERVER_ENDPOINT is required; DSHX never falls back to the Codex agent runtime"
+            ))?;
+        let dshx_endpoint = resolve_remote_addr(&dshx_endpoint)?;
         let exit_info = run_main(
             inner,
             arg0_paths,
             LoaderOverrides::default(),
-            /*explicit_remote_endpoint*/ None,
+            /*explicit_remote_endpoint*/ Some(dshx_endpoint),
         )
         .await?;
         let is_fatal = match &exit_info.exit_reason {
